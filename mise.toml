# Toolchain pin the language-tools SC9 check reads before it runs build/test/lint/
# format/vet. Pinned at the K8 floor (go 1.26.0); the SC9a comparison rule passes any
# resolved Go toolchain that equals or exceeds this version.
[tools]
go = "1.26.0"
