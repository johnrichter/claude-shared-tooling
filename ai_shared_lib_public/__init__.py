"""ai_shared_lib_public — public, dependency-free shared tools for AI-agent workspaces.

Every module here is stdlib-only (Tier 0): no third-party runtime dependency, no
network install needed to import or run. Consumed by path dependency in dev and by
git-tag dependency when shipped.
"""

__version__ = "0.1.0"
