// Package fsx provides safe filesystem operations: crash-safe atomic writes,
// rename-based moves, injected-ruleset path classification, cruft detection,
// and drift scans between a directory's actual contents and the enumerators
// meant to account for them.
package fsx
