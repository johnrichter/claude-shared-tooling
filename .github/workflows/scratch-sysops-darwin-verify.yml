name: Scratch — sysops darwin verify
on:
  push:
    branches: [chore/sysops-darwin-guard]

jobs:
  test:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.27.1"
      - run: cd go/sysops && go test -v ./...
